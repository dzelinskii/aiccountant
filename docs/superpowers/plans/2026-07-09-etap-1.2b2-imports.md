# Этап 1.2b2 «Импорт выписки Т-Банка (PDF)» — план реализации

> **Для агентов-исполнителей:** ОБЯЗАТЕЛЬНАЯ САБ-СКИЛЛ —
> superpowers:subagent-driven-development (рекомендуется) либо
> superpowers:executing-plans. Шаги отмечаются чекбоксами (`- [ ]`).

**Цель:** импорт PDF-выписки «Справка о движении средств» Т-Банка —
детерминированный парсер под фиксированный формат, синхронный поток
превью→подтверждение, дедуп по хешу; импортированные операции без категории
(`source=import`).

**Архитектура:** новый модуль `app/imports/`. Парсер двухслойный:
`extract_lines` (pypdf, тонкий) + чистая `parse_statement` (тестируется на
текстовых фикстурах реального формата). Транзакции создаются только через
`ledger.service.post_transaction` (source=import), с проставлением
`external_id`/`import_id`. Дедуп — по `(account_id, external_id)` через новый
запрос ledger. Границы держит import-linter.

**Стек:** FastAPI, SQLAlchemy 2 async, Alembic, Pydantic v2, **pypdf** (парсинг),
pytest + testcontainers, **reportlab** (dev — синтетический PDF для теста);
React 19 + TS + Vite, Mantine v9, vitest.

**Ключевые решения (из спеки):**

- Источник — PDF; парсер детерминированный под формат Т-Банка; произвольные
  форматы/LLM — этап 2.
- Дедуп: `external_id = sha256(account_id|occurred_at|amount|description)`,
  уникальный индекс `(account_id, external_id) WHERE external_id IS NOT NULL`.
- Синхронно: `POST /api/imports?...&commit=false|true` (multipart), парсинг на
  каждом вызове; коммит вставляет только новые.
- Импортированные операции: `source=import`, `category_id=null`, знак из выписки.
- Постинг через `ledger.service`; `imports` не трогает таблицы ledger.

**Порядок фаз:** Фаза 0 (зависимости, миграция+модели) → Фаза 1 (парсер:
чистый `parse_statement`, затем `extract_lines`+синтетический PDF) → Фаза 2
(backend: расширение ledger.service, вертикаль imports) → Фаза 3 (frontend).

## Структура файлов

- `backend/pyproject.toml` — зависимости pypdf (+ reportlab в dev), mypy-overrides.
- `backend/app/imports/models.py` — `Import` (таблица `imports`).
- `backend/app/ledger/models.py` — `Transaction` + `external_id`/`import_id` + уникальный индекс.
- `backend/alembic/versions/0005_imports.py` — миграция.
- `backend/app/imports/parser.py` — `extract_lines`, `parse_statement`, dataclasses, `StatementParseError`.
- `backend/app/imports/schemas.py` — DTO превью/результата.
- `backend/app/imports/repository.py` — работа с таблицей `imports`.
- `backend/app/imports/service.py` — превью/коммит импорта, хеш `external_id`.
- `backend/app/imports/router.py` — эндпоинт `/api/imports`.
- `backend/app/ledger/service.py` — `post_transaction` (+external_id/import_id), `existing_external_ids`.
- `backend/tests/fixtures.py` — `make_statement_pdf(...)` (reportlab) для тестов.
- `frontend/src/api/imports.ts`, `frontend/src/pages/ImportPage.tsx` — фронт.

---

## Фаза 0. Фундамент

### Задача 1: Зависимости (pypdf + reportlab)

**Файлы:**
- Modify: `backend/pyproject.toml`

- [ ] **Шаг 1: Добавить зависимости и mypy-override**

В `backend/pyproject.toml` в `[project].dependencies` добавить `"pypdf>=5.1"`.
В `[dependency-groups].dev` добавить `"reportlab>=4.2"` (генерация синтетического
PDF в тестах). В секцию mypy добавить override (у обеих библиотек типы неполные):

```toml
[[tool.mypy.overrides]]
module = ["pypdf", "pypdf.*", "reportlab", "reportlab.*"]
ignore_missing_imports = true
```

Run: `cd backend && uv sync` (обновит `uv.lock`).

- [ ] **Шаг 2: Проверить импорт pypdf**

Run: `cd backend && uv run python -c "import pypdf, reportlab; print('ok')"`
Expected: `ok`.

- [ ] **Шаг 3: Backend-прогон не сломан**

Run: `cd backend && uv run ruff check . && uv run mypy && uv run pytest -q`
Expected: зелёное (существующие тесты).

- [ ] **Шаг 4: Коммит**

```bash
git add backend/pyproject.toml backend/uv.lock
git commit -m "Imports: зависимости pypdf (парсинг) и reportlab (dev, синтетический PDF)"
```

---

### Задача 2: Миграция `0005` — таблица `imports`, поля транзакций

**Файлы:**
- Create: `backend/app/imports/__init__.py`
- Create: `backend/app/imports/models.py`
- Modify: `backend/app/ledger/models.py` (Transaction + external_id/import_id + индекс)
- Modify: `backend/alembic/env.py` (импорт моделей imports)
- Create: `backend/alembic/versions/0005_imports.py`
- Test: `backend/tests/test_migrations.py`

- [ ] **Шаг 1: Пустой пакет**

Create `backend/app/imports/__init__.py` (пустой).

- [ ] **Шаг 2: Модель `Import`**

Create `backend/app/imports/models.py`:

```python
import uuid
from datetime import datetime

from sqlalchemy import DateTime, ForeignKey, String, func
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.orm import Mapped, mapped_column

from app.core.db import Base


class Import(Base):
    __tablename__ = "imports"

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    workspace_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("workspaces.id"))
    account_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("accounts.id"))
    file_name: Mapped[str] = mapped_column(String(300))
    bank_profile: Mapped[str] = mapped_column(String(30))
    status: Mapped[str] = mapped_column(String(20))
    stats: Mapped[dict[str, int]] = mapped_column(JSONB)
    created_by: Mapped[uuid.UUID] = mapped_column(ForeignKey("users.id"))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
```

- [ ] **Шаг 3: Transaction — external_id/import_id + уникальный индекс**

В `backend/app/ledger/models.py`: добавить импорт `text` (`from sqlalchemy import ... , text`), в класс `Transaction` добавить поля (после `transfer_group_id`):

```python
    # дедуп импорта: хеш операции; мягкая ссылка на запись imports (без FK,
    # чтобы ledger не зависел от модуля imports)
    external_id: Mapped[str | None] = mapped_column(String(64), nullable=True)
    import_id: Mapped[uuid.UUID | None] = mapped_column(nullable=True)
```

и в `__table_args__` добавить частичный уникальный индекс (рядом с двумя
существующими):

```python
        Index(
            "uq_transactions_account_external",
            "account_id",
            "external_id",
            unique=True,
            postgresql_where=text("external_id IS NOT NULL"),
        ),
```

- [ ] **Шаг 4: Зарегистрировать модели imports в alembic env**

В `backend/alembic/env.py` рядом с импортами моделей добавить:

```python
from app.imports import models as imports_models  # noqa: F401  (регистрирует таблицы)
```

- [ ] **Шаг 5: Миграция (автоген + вычитка)**

Run: `cd backend && uv run alembic revision --autogenerate -m "imports table and transaction dedup fields" --rev-id 0005`
Вычитать, переименовать в `backend/alembic/versions/0005_imports.py`: `down_revision = "0004"`; создание таблицы `imports` (JSONB для stats, FK на workspaces/accounts/users, имена через `op.f`); `add_column` `external_id`(String(64))/`import_id`(Uuid) в `transactions`; создание уникального частичного индекса
`uq_transactions_account_external` (`postgresql_where=sa.text("external_id IS NOT NULL")`). Стиль — как `0003_recurring.py`.
Прогнать `uv run alembic check` (Docker) → `No new upgrade operations detected`.

- [ ] **Шаг 6: Тест миграции**

В `backend/tests/test_migrations.py` добавить:

```python
async def test_migrations_create_imports_and_dedup(database_url: str) -> None:
    engine = create_async_engine(database_url)
    async with engine.connect() as conn:
        assert (await conn.execute(text("SELECT to_regclass('imports')"))).scalar() == "imports"
        cols = await conn.execute(
            text(
                "SELECT column_name FROM information_schema.columns "
                "WHERE table_name = 'transactions' "
                "AND column_name IN ('external_id', 'import_id')"
            )
        )
        assert {r[0] for r in cols} == {"external_id", "import_id"}
    await engine.dispose()
```

- [ ] **Шаг 7: Прогон**

Run: `cd backend && uv run pytest tests/test_migrations.py -q && uv run mypy && uv run ruff check .`
Expected: PASS; mypy/ruff чисто.

- [ ] **Шаг 8: Коммит**

```bash
git add backend/app/imports/__init__.py backend/app/imports/models.py \
        backend/app/ledger/models.py backend/alembic/env.py \
        backend/alembic/versions/0005_imports.py backend/tests/test_migrations.py
git commit -m "Imports: таблица imports и поля дедупа external_id/import_id (миграция 0005)"
```

---

## Фаза 1. Парсер

### Задача 3: Чистая функция `parse_statement`

Ядро разбора: группирует извлечённые строки в операции. Не зависит от PDF и БД,
покрывается юнит-тестами на фикстурах реального формата Т-Банка.

**Файлы:**
- Create: `backend/app/imports/parser.py`
- Test: `backend/tests/test_statement_parser.py`

- [ ] **Шаг 1: Тесты (падают)**

Create `backend/tests/test_statement_parser.py`:

```python
from datetime import date
from decimal import Decimal

import pytest

from app.imports.parser import StatementParseError, parse_statement

# строки в формате реального извлечения pypdf из «Справки о движении средств»
SAMPLE = [
    "Движение средств за период с 06.06.2026 по 06.07.2026",
    "Дата и время", "операции", "Номер", "карты",
    "04.07.2026", "12:37", "04.07.2026", "12:37",
    "-1 150.00 ₽ -1 150.00 ₽ Внешний перевод по", "номеру телефона", "+79897050701", "9358",
    "02.07.2026", "17:12", "02.07.2026", "17:12",
    "+5 000.00 ₽ +5 000.00 ₽ Пополнение. Система", "быстрых платежей", "9358",
    "10.06.2026", "13:03", "10.06.2026", "13:03",
    "-14 405.33 ₽ -14 405.33 ₽ Пополнение Кубышки 9358",
    "451 358,48 ₽Пополнения:",
    "502 119,39 ₽Расходы:",
]


def test_parses_all_operations() -> None:
    st = parse_statement(SAMPLE)
    assert len(st.operations) == 3


def test_signed_amounts_and_dates() -> None:
    ops = parse_statement(SAMPLE).operations
    assert ops[0].occurred_at == date(2026, 7, 4)
    assert ops[0].amount == Decimal("-1150.00")
    assert ops[0].currency == "RUB"
    assert ops[1].amount == Decimal("5000.00")  # доход, знак +


def test_multiline_description_joined_card_stripped() -> None:
    ops = parse_statement(SAMPLE).operations
    assert ops[0].description == "Внешний перевод по номеру телефона +79897050701"
    # описание и номер карты на одной строке — карта отрезается
    assert ops[2].description == "Пополнение Кубышки"


def test_totals_control_sum() -> None:
    st = parse_statement(SAMPLE)
    assert st.total_income == Decimal("451358.48")
    assert st.total_expense == Decimal("502119.39")


def test_empty_or_garbage_raises() -> None:
    with pytest.raises(StatementParseError):
        parse_statement(["мусор", "нет операций"])
```

- [ ] **Шаг 2: Убедиться, что падают**

Run: `cd backend && uv run pytest tests/test_statement_parser.py -q`
Expected: FAIL (модуля нет).

- [ ] **Шаг 3: Реализация парсера (чистая часть)**

Create `backend/app/imports/parser.py`:

```python
import re
from dataclasses import dataclass
from datetime import date
from decimal import Decimal

DATE_RE = re.compile(r"^\d{2}\.\d{2}\.\d{4}$")
TIME_RE = re.compile(r"^\d{2}:\d{2}$")
# строка сумм: сумма в валюте операции ₽, сумма в валюте карты ₽, затем начало описания
AMOUNT_LINE_RE = re.compile(
    r"^([+-][\d\s\xa0]+[.,]\d{2})\s*₽\s+([+-][\d\s\xa0]+[.,]\d{2})\s*₽\s*(.*)$"
)
CARD_RE = re.compile(r"^\d{3,4}$")
TOTAL_RE = re.compile(r"([\d\s\xa0]+[.,]\d{2})\s*₽")


class StatementParseError(Exception):
    """Не удалось разобрать выписку (неверный формат)."""


@dataclass
class ParsedOperation:
    occurred_at: date
    amount: Decimal  # знаковая: расход < 0, доход > 0
    currency: str
    description: str


@dataclass
class ParsedStatement:
    operations: list[ParsedOperation]
    total_income: Decimal | None
    total_expense: Decimal | None


def _money(raw: str) -> Decimal:
    return Decimal(raw.replace(" ", "").replace("\xa0", "").replace(",", "."))


def _parse_date(raw: str) -> date:
    d, m, y = raw.split(".")
    return date(int(y), int(m), int(d))


def _is_block_start(lines: list[str], j: int, n: int) -> bool:
    # операция начинается с блока: дата/время/дата/время/строка-сумма
    return (
        j + 4 < n
        and bool(DATE_RE.match(lines[j]))
        and bool(TIME_RE.match(lines[j + 1]))
        and bool(DATE_RE.match(lines[j + 2]))
        and bool(TIME_RE.match(lines[j + 3]))
        and bool(AMOUNT_LINE_RE.match(lines[j + 4]))
    )


def _clean_description(text: str) -> str:
    words = text.split()
    if words and re.fullmatch(r"\d{3,4}", words[-1]):
        words = words[:-1]  # хвостовой номер карты
    return " ".join(words).strip()


def parse_statement(raw_lines: list[str]) -> ParsedStatement:
    lines = [line.strip() for line in raw_lines if line.strip()]
    n = len(lines)
    operations: list[ParsedOperation] = []
    total_income: Decimal | None = None
    total_expense: Decimal | None = None

    i = 0
    while i < n:
        line = lines[i]
        if "Пополнения:" in line:
            match = TOTAL_RE.search(line)
            if match:
                total_income = _money(match.group(1))
            i += 1
            continue
        if "Расходы:" in line:
            match = TOTAL_RE.search(line)
            if match:
                total_expense = _money(match.group(1))
            i += 1
            continue
        if _is_block_start(lines, i, n):
            occurred_at = _parse_date(lines[i])
            amount_match = AMOUNT_LINE_RE.match(lines[i + 4])
            assert amount_match is not None
            amount = _money(amount_match.group(2))
            desc_parts = [amount_match.group(3)] if amount_match.group(3).strip() else []
            j = i + 5
            while (
                j < n
                and not _is_block_start(lines, j, n)
                and "Пополнения:" not in lines[j]
                and "Расходы:" not in lines[j]
            ):
                desc_parts.append(lines[j])
                j += 1
            operations.append(
                ParsedOperation(
                    occurred_at=occurred_at,
                    amount=amount,
                    currency="RUB",
                    description=_clean_description(" ".join(desc_parts)),
                )
            )
            i = j
            continue
        i += 1

    if not operations:
        raise StatementParseError("не найдено ни одной операции — неверный формат выписки")
    return ParsedStatement(operations, total_income, total_expense)
```

- [ ] **Шаг 4: Тесты зелёные + линт/типы**

Run: `cd backend && uv run pytest tests/test_statement_parser.py -q && uv run mypy && uv run ruff check .`
Expected: 5 passed; mypy/ruff чисто (при необходимости `uv run ruff format .`).

- [ ] **Шаг 5: Коммит**

```bash
git add backend/app/imports/parser.py backend/tests/test_statement_parser.py
git commit -m "Imports: чистый парсер parse_statement выписки Т-Банка (текстовые фикстуры)"
```

> **Важно про тестовую стратегию (проверено эмпирически):** reportlab на
> стандартном шрифте Helvetica НЕ рендерит `₽` (выходит `■`), а зависеть от
> системного TTF в тестах ненадёжно. Поэтому `₽`-PDF в тестах НЕ генерируем:
> `extract_lines` тестируется простым ASCII-PDF (обёртка pypdf), а API-тесты
> подменяют `extract_lines` каноническими строками `SAMPLE` (эти фикстуры
> построены на реальном выводе `pypdf.extract_text` из настоящей выписки).

---

### Задача 4: `extract_lines` (pypdf) + фикстура PDF

**Файлы:**
- Modify: `backend/app/imports/parser.py` (добавить `extract_lines`)
- Create: `backend/tests/fixtures.py` (`make_simple_pdf`)
- Test: `backend/tests/test_statement_parser.py` (тест extract_lines)

- [ ] **Шаг 1: Тест (падает)**

В `backend/tests/test_statement_parser.py` добавить (вверх — импорт хелпера):

```python
from tests.fixtures import make_simple_pdf
from app.imports.parser import extract_lines
```

и тест:

```python
def test_extract_lines_reads_pdf_text() -> None:
    pdf = make_simple_pdf(["03.07.2026", "12:37", "Оплата в кафе"])
    lines = extract_lines(pdf)
    assert "03.07.2026" in lines
    assert "Оплата в кафе" in lines
```

- [ ] **Шаг 2: Хелпер генерации PDF**

Create `backend/tests/fixtures.py`:

```python
import io

from reportlab.pdfgen import canvas


def make_simple_pdf(lines: list[str]) -> bytes:
    """Собрать простой PDF: по строке на drawString (pypdf извлечёт их построчно).
    Только для теста обёртки extract_lines; символ ₽ здесь не используется —
    стандартный шрифт reportlab его не рендерит (см. стратегию тестов)."""
    buf = io.BytesIO()
    pdf = canvas.Canvas(buf)
    y = 800
    for line in lines:
        pdf.drawString(40, y, line)
        y -= 16
    pdf.save()
    return buf.getvalue()
```

- [ ] **Шаг 3: `extract_lines` в parser.py**

В `backend/app/imports/parser.py` добавить импорт `import io` и
`from pypdf import PdfReader` (вверху) и функцию:

```python
def extract_lines(pdf_bytes: bytes) -> list[str]:
    """Тонкий слой: извлечь текст из PDF и разрезать на строки. Вся зависимость
    от pypdf/формата PDF здесь; разбор — в parse_statement."""
    reader = PdfReader(io.BytesIO(pdf_bytes))
    lines: list[str] = []
    for page in reader.pages:
        text = page.extract_text() or ""
        lines.extend(text.splitlines())
    return lines
```

- [ ] **Шаг 4: Тесты зелёные**

Run: `cd backend && uv run pytest tests/test_statement_parser.py -q && uv run mypy && uv run ruff check .`
Expected: 6 passed; mypy/ruff чисто.

- [ ] **Шаг 5: Коммит**

```bash
git add backend/app/imports/parser.py backend/tests/fixtures.py backend/tests/test_statement_parser.py
git commit -m "Imports: extract_lines (pypdf) и PDF-фикстура для тестов"
```

---

## Фаза 2. Backend: сервис и API

### Задача 5: Расширение `ledger.service` (external_id/import_id, дедуп)

Постинг импортированных операций и проверка дублей идут через публичный сервис
ledger (imports не трогает таблицы ledger).

**Файлы:**
- Modify: `backend/app/ledger/service.py`
- Modify: `backend/app/ledger/repository.py`
- Test: `backend/tests/test_import_dedup_service.py`

- [ ] **Шаг 1: Тест (падает)**

Create `backend/tests/test_import_dedup_service.py`:

```python
import uuid
from datetime import date
from decimal import Decimal

from httpx import AsyncClient
from sqlalchemy.ext.asyncio import AsyncSession

from app.ledger import service

ALICE = {"email": "alice@example.com", "password": "password123"}


async def test_post_with_external_id_and_dedup_lookup(client: AsyncClient, db_session: AsyncSession) -> None:
    reg = await client.post("/api/auth/register", json=ALICE)
    user_id = uuid.UUID(reg.json()["id"])
    me = await client.get("/api/me")
    ws = uuid.UUID(me.json()["workspaces"][0]["id"])
    acc = uuid.UUID(
        (await client.post("/api/accounts", params={"workspace_id": str(ws)},
         json={"name": "Карта", "type": "card", "currency": "RUB"})).json()["id"]
    )

    txn = await service.post_transaction(
        db_session, ws, user_id, account_id=acc, category_id=None,
        amount=Decimal("-100.00"), occurred_at=date(2026, 7, 5), source="import",
        external_id="hash-1", import_id=uuid.uuid4(),
    )
    await db_session.commit()
    assert txn.external_id == "hash-1"
    assert txn.source == "import"
    assert txn.category_id is None

    found = await service.existing_external_ids(db_session, ws, acc, {"hash-1", "hash-2"})
    assert found == {"hash-1"}
```

- [ ] **Шаг 2: Убедиться, что падает**

Run: `cd backend && uv run pytest tests/test_import_dedup_service.py -q`
Expected: FAIL (`post_transaction` не знает `external_id`; нет `existing_external_ids`).

- [ ] **Шаг 3: `post_transaction` — external_id/import_id**

В `backend/app/ledger/service.py` в `post_transaction` добавить два параметра
(после `note`) и проставить их в `Transaction`:

```python
    external_id: str | None = None,
    import_id: uuid.UUID | None = None,
```

В конструкторе `Transaction(...)` добавить поля:

```python
        external_id=external_id,
        import_id=import_id,
```

- [ ] **Шаг 4: Repository — дедуп-запрос**

В `backend/app/ledger/repository.py` добавить:

```python
async def existing_external_ids(
    db: AsyncSession, workspace_id: uuid.UUID, account_id: uuid.UUID, external_ids: set[str]
) -> set[str]:
    if not external_ids:
        return set()
    rows = await db.execute(
        select(Transaction.external_id).where(
            Transaction.workspace_id == workspace_id,
            Transaction.account_id == account_id,
            Transaction.external_id.in_(external_ids),
        )
    )
    return {value for (value,) in rows.all() if value is not None}
```

- [ ] **Шаг 5: Service — обёртки `existing_external_ids` и `account_exists`**

В `backend/app/ledger/service.py` добавить:

```python
async def existing_external_ids(
    db: AsyncSession, workspace_id: uuid.UUID, account_id: uuid.UUID, external_ids: set[str]
) -> set[str]:
    return await repository.existing_external_ids(db, workspace_id, account_id, external_ids)


async def account_exists(db: AsyncSession, workspace_id: uuid.UUID, account_id: uuid.UUID) -> bool:
    return await repository.get_account(db, workspace_id, account_id) is not None
```

Дополнить тест `test_import_dedup_service.py` проверкой:

```python
    assert await service.account_exists(db_session, ws, acc) is True
    assert await service.account_exists(db_session, ws, uuid.uuid4()) is False
```

- [ ] **Шаг 6: Прогон**

Run: `cd backend && uv run pytest tests/test_import_dedup_service.py -q && uv run ruff check . && uv run mypy && uv run lint-imports && uv run pytest -q`
Expected: зелёное; `4 kept` (существующие тесты транзакций не сломаны —
новые параметры опциональны).

- [ ] **Шаг 7: Коммит**

```bash
git add backend/app/ledger/service.py backend/app/ledger/repository.py \
        backend/tests/test_import_dedup_service.py
git commit -m "Ledger: post_transaction принимает external_id/import_id; дедуп-запрос"
```

---

### Задача 6: Вертикаль imports (schemas, service, router) + границы

**Файлы:**
- Create: `backend/app/imports/schemas.py`
- Create: `backend/app/imports/repository.py`
- Create: `backend/app/imports/service.py`
- Create: `backend/app/imports/router.py`
- Modify: `backend/app/main.py` (подключить роутер)
- Modify: `backend/pyproject.toml` (контракты import-linter для imports)
- Test: `backend/tests/test_imports_api.py`

- [ ] **Шаг 1: Тесты API (падают)**

Create `backend/tests/test_imports_api.py`:

```python
from httpx import AsyncClient

ALICE = {"email": "alice@example.com", "password": "password123"}
BOB = {"email": "bob@example.com", "password": "password123"}

SAMPLE = [
    "04.07.2026", "12:37", "04.07.2026", "12:37",
    "-1 150.00 ₽ -1 150.00 ₽ Внешний перевод по", "номеру телефона", "+79897050701", "9358",
    "02.07.2026", "17:12", "02.07.2026", "17:12",
    "+5 000.00 ₽ +5 000.00 ₽ Пополнение. Система", "быстрых платежей", "9358",
    "10.06.2026", "13:03", "10.06.2026", "13:03",
    "-14 405.33 ₽ -14 405.33 ₽ Пополнение Кубышки 9358",
    "451 358,48 ₽Пополнения:", "502 119,39 ₽Расходы:",
]
FILES = {"file": ("statement.pdf", b"%PDF-dummy", "application/pdf")}


async def _setup(client: AsyncClient, creds: dict[str, str]) -> tuple[str, str]:
    await client.post("/api/auth/register", json=creds)
    me = await client.get("/api/me")
    ws = str(me.json()["workspaces"][0]["id"])
    acc = (await client.post("/api/accounts", params={"workspace_id": ws},
           json={"name": "Карта", "type": "card", "currency": "RUB"})).json()["id"]
    return ws, acc


async def test_preview_counts(client: AsyncClient, monkeypatch) -> None:
    monkeypatch.setattr("app.imports.service.extract_lines", lambda b: SAMPLE)
    ws, acc = await _setup(client, ALICE)
    resp = await client.post(
        "/api/imports", params={"workspace_id": ws, "account_id": acc, "commit": "false"}, files=FILES
    )
    assert resp.status_code == 200
    body = resp.json()
    assert body["new_count"] == 3
    assert body["duplicate_count"] == 0
    assert body["operations"][0]["amount"] == "-1150.0000"
    assert body["operations"][1]["amount"] == "5000.0000"
    assert all(op["is_duplicate"] is False for op in body["operations"])


async def test_commit_inserts_and_reimport_is_deduped(client: AsyncClient, monkeypatch) -> None:
    monkeypatch.setattr("app.imports.service.extract_lines", lambda b: SAMPLE)
    ws, acc = await _setup(client, ALICE)

    r1 = await client.post(
        "/api/imports", params={"workspace_id": ws, "account_id": acc, "commit": "true"}, files=FILES
    )
    assert r1.status_code == 200
    assert r1.json()["imported"] == 3

    txns = (await client.get("/api/transactions", params={"workspace_id": ws})).json()
    assert txns["total"] == 3
    assert all(t["category_id"] is None for t in txns["items"])

    r2 = await client.post(
        "/api/imports", params={"workspace_id": ws, "account_id": acc, "commit": "true"}, files=FILES
    )
    assert r2.json()["imported"] == 0
    assert r2.json()["duplicates"] == 3
    after = (await client.get("/api/transactions", params={"workspace_id": ws})).json()
    assert after["total"] == 3  # повторный импорт не задвоил


async def test_uncategorized_import_in_dashboard_bucket(client: AsyncClient, monkeypatch) -> None:
    monkeypatch.setattr("app.imports.service.extract_lines", lambda b: SAMPLE)
    ws, acc = await _setup(client, ALICE)
    await client.post(
        "/api/imports", params={"workspace_id": ws, "account_id": acc, "commit": "true"}, files=FILES
    )
    dash = (await client.get("/api/dashboard", params={"workspace_id": ws})).json()
    # расходы без категории сведены в бакет «Без категории» (если попали в текущий месяц —
    # проверяем сам факт наличия бакета среди расходов, суммы зависят от дат выписки)
    assert any(m["category_id"] is None for m in dash["month_expenses"]) or dash["month_expenses"] == []


async def test_foreign_account_rejected(client: AsyncClient, monkeypatch) -> None:
    monkeypatch.setattr("app.imports.service.extract_lines", lambda b: SAMPLE)
    ws_a, acc_a = await _setup(client, ALICE)
    client.cookies.clear()
    ws_b, _ = await _setup(client, BOB)
    # Боб импортирует в чужой счёт под своим workspace → 404
    resp = await client.post(
        "/api/imports", params={"workspace_id": ws_b, "account_id": acc_a, "commit": "false"}, files=FILES
    )
    assert resp.status_code == 404
    # чужой workspace → 403
    resp2 = await client.post(
        "/api/imports", params={"workspace_id": ws_a, "account_id": acc_a, "commit": "false"}, files=FILES
    )
    assert resp2.status_code == 403


async def test_unparsable_pdf_422(client: AsyncClient, monkeypatch) -> None:
    monkeypatch.setattr("app.imports.service.extract_lines", lambda b: ["мусор", "нет операций"])
    ws, acc = await _setup(client, ALICE)
    resp = await client.post(
        "/api/imports", params={"workspace_id": ws, "account_id": acc, "commit": "false"}, files=FILES
    )
    assert resp.status_code == 422
```

- [ ] **Шаг 2: Убедиться, что падают**

Run: `cd backend && uv run pytest tests/test_imports_api.py -q`
Expected: FAIL (модуля/эндпоинта нет).

- [ ] **Шаг 3: Схемы**

Create `backend/app/imports/schemas.py`:

```python
import uuid
from datetime import date

from pydantic import BaseModel

from app.core.money import MoneyStr


class ImportOperationOut(BaseModel):
    occurred_at: date
    amount: MoneyStr
    currency: str
    description: str
    is_duplicate: bool


class ImportPreviewOut(BaseModel):
    operations: list[ImportOperationOut]
    new_count: int
    duplicate_count: int
    total_income: MoneyStr | None
    total_expense: MoneyStr | None


class ImportResultOut(BaseModel):
    import_id: uuid.UUID
    imported: int
    duplicates: int
```

- [ ] **Шаг 4: Repository**

Create `backend/app/imports/repository.py`:

```python
from sqlalchemy.ext.asyncio import AsyncSession

from app.imports.models import Import


def add_import(db: AsyncSession, imp: Import) -> None:
    db.add(imp)
```

- [ ] **Шаг 5: Service**

Create `backend/app/imports/service.py`:

```python
import hashlib
import uuid

from sqlalchemy.ext.asyncio import AsyncSession

from app.imports import repository
from app.imports.models import Import
from app.imports.parser import ParsedOperation, ParsedStatement, extract_lines, parse_statement
from app.imports.schemas import ImportOperationOut, ImportPreviewOut, ImportResultOut
from app.ledger import service as ledger_service

BANK_PROFILE = "tbank_statement"


def _external_id(account_id: uuid.UUID, op: ParsedOperation) -> str:
    raw = f"{account_id}|{op.occurred_at.isoformat()}|{op.amount}|{op.description}"
    return hashlib.sha256(raw.encode("utf-8")).hexdigest()


def _parse(pdf_bytes: bytes) -> ParsedStatement:
    return parse_statement(extract_lines(pdf_bytes))  # может бросить StatementParseError


async def preview(
    db: AsyncSession, workspace_id: uuid.UUID, account_id: uuid.UUID, pdf_bytes: bytes
) -> ImportPreviewOut:
    statement = _parse(pdf_bytes)
    ext_ids = [_external_id(account_id, op) for op in statement.operations]
    existing = await ledger_service.existing_external_ids(db, workspace_id, account_id, set(ext_ids))
    seen: set[str] = set()
    operations: list[ImportOperationOut] = []
    new_count = 0
    for op, eid in zip(statement.operations, ext_ids, strict=True):
        is_duplicate = eid in existing or eid in seen
        seen.add(eid)
        if not is_duplicate:
            new_count += 1
        operations.append(
            ImportOperationOut(
                occurred_at=op.occurred_at,
                amount=op.amount,
                currency=op.currency,
                description=op.description,
                is_duplicate=is_duplicate,
            )
        )
    return ImportPreviewOut(
        operations=operations,
        new_count=new_count,
        duplicate_count=len(operations) - new_count,
        total_income=statement.total_income,
        total_expense=statement.total_expense,
    )


async def commit_import(
    db: AsyncSession,
    workspace_id: uuid.UUID,
    account_id: uuid.UUID,
    user_id: uuid.UUID,
    file_name: str,
    pdf_bytes: bytes,
) -> ImportResultOut:
    statement = _parse(pdf_bytes)
    ext_ids = [_external_id(account_id, op) for op in statement.operations]
    existing = await ledger_service.existing_external_ids(db, workspace_id, account_id, set(ext_ids))

    imp = Import(
        workspace_id=workspace_id,
        account_id=account_id,
        file_name=file_name,
        bank_profile=BANK_PROFILE,
        status="completed",
        stats={},
        created_by=user_id,
    )
    repository.add_import(db, imp)
    await db.flush()  # получить imp.id

    seen: set[str] = set()
    imported = 0
    for op, eid in zip(statement.operations, ext_ids, strict=True):
        if eid in existing or eid in seen:
            continue
        seen.add(eid)
        await ledger_service.post_transaction(
            db, workspace_id, user_id,
            account_id=account_id, category_id=None, amount=op.amount,
            occurred_at=op.occurred_at, source="import",
            merchant=op.description[:300] or None,
            external_id=eid, import_id=imp.id,
        )
        imported += 1

    duplicates = len(statement.operations) - imported
    imp.stats = {"parsed": len(statement.operations), "imported": imported, "duplicates": duplicates}
    await db.commit()  # запись импорта и операции — один commit
    return ImportResultOut(import_id=imp.id, imported=imported, duplicates=duplicates)
```

- [ ] **Шаг 6: Router + подключение в main**

Create `backend/app/imports/router.py`:

```python
import uuid
from typing import Annotated

from fastapi import APIRouter, Depends, HTTPException, UploadFile
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.db import get_db
from app.identity.deps import require_workspace_member
from app.identity.models import User
from app.imports import service
from app.imports.parser import StatementParseError
from app.imports.schemas import ImportPreviewOut, ImportResultOut
from app.ledger import service as ledger_service

router = APIRouter(prefix="/api")


@router.post("/imports")
async def import_statement(
    workspace_id: uuid.UUID,
    account_id: uuid.UUID,
    user: Annotated[User, Depends(require_workspace_member)],
    db: Annotated[AsyncSession, Depends(get_db)],
    file: UploadFile,
    commit: bool = False,
) -> ImportPreviewOut | ImportResultOut:
    if not await ledger_service.account_exists(db, workspace_id, account_id):
        raise HTTPException(status_code=404, detail="Счёт не найден")
    pdf_bytes = await file.read()
    try:
        if commit:
            return await service.commit_import(
                db, workspace_id, account_id, user.id, file.filename or "statement.pdf", pdf_bytes
            )
        return await service.preview(db, workspace_id, account_id, pdf_bytes)
    except StatementParseError:
        raise HTTPException(status_code=422, detail="Не удалось разобрать выписку") from None
```

В `backend/app/main.py` добавить импорт и include (после recurring-роутера):

```python
from app.imports.router import router as imports_router
```
```python
app.include_router(imports_router)
```

- [ ] **Шаг 7: import-linter — границы imports**

В `backend/pyproject.toml` в секцию `[tool.importlinter]` добавить два контракта:

```toml
[[tool.importlinter.contracts]]
name = "imports не лезет во внутренности identity и ledger"
type = "forbidden"
source_modules = ["app.imports.service", "app.imports.repository", "app.imports.parser"]
forbidden_modules = ["app.identity", "app.ledger.repository", "app.ledger.models"]
ignore_imports = ["app.imports.service -> app.ledger.service"]

[[tool.importlinter.contracts]]
name = "identity и ledger не зависят от imports"
type = "forbidden"
source_modules = [
    "app.identity.service", "app.identity.models", "app.identity.deps",
    "app.ledger.service", "app.ledger.repository", "app.ledger.models",
]
forbidden_modules = ["app.imports"]
```

- [ ] **Шаг 8: Прогон**

Run: `cd backend && uv run pytest tests/test_imports_api.py -q`
Expected: PASS (превью-счётчики; коммит вставляет 3, повторный — 0/дубли;
некатегоризированные в бакете дашборда; чужой счёт 404 / чужой workspace 403;
битый PDF 422).

- [ ] **Шаг 9: Линт, типы, import-linter (6 контрактов), весь набор**

Run: `cd backend && uv run ruff check . && uv run ruff format --check . && uv run mypy && uv run lint-imports && uv run pytest -q`
Expected: всё зелёное; `Contracts: 6 kept, 0 broken`. Проверить непустоту нового
контракта пробой: временно `from app.ledger import repository as _p  # noqa` в
`app/imports/service.py` → `lint-imports` показывает BROKEN на imports-контракте;
убрать пробу → снова 6 kept. Зафиксировать в отчёте.

- [ ] **Шаг 10: Коммит**

```bash
git add backend/app/imports/schemas.py backend/app/imports/repository.py \
        backend/app/imports/service.py backend/app/imports/router.py \
        backend/app/main.py backend/pyproject.toml backend/tests/test_imports_api.py
git commit -m "Imports: превью и импорт выписки (дедуп, source=import, границы модуля)"
```

---

## Фаза 3. Frontend

### Задача 7: api-модуль, страница «Импорт», навигация

**Файлы:**
- Create: `frontend/src/api/imports.ts`
- Create: `frontend/src/pages/ImportPreviewPanel.tsx`
- Create: `frontend/src/pages/ImportPage.tsx`
- Modify: `frontend/src/AppLayout.tsx` (навигация), `frontend/src/main.tsx` (роут)
- Test: `frontend/src/pages/ImportPreviewPanel.test.tsx`

- [ ] **Шаг 1: api-модуль (multipart через fetch)**

Create `frontend/src/api/imports.ts`:

```typescript
import { ApiError } from './client'

export interface ImportOperation {
  occurred_at: string
  amount: string
  currency: string
  description: string
  is_duplicate: boolean
}

export interface ImportPreview {
  operations: ImportOperation[]
  new_count: number
  duplicate_count: number
  total_income: string | null
  total_expense: string | null
}

export interface ImportResult {
  import_id: string
  imported: number
  duplicates: number
}

// multipart: не выставляем Content-Type вручную — браузер сам добавит boundary
async function upload<T>(ws: string, accountId: string, file: File, commit: boolean): Promise<T> {
  const form = new FormData()
  form.append('file', file)
  const qs = new URLSearchParams({ workspace_id: ws, account_id: accountId, commit: String(commit) })
  const res = await fetch(`/api/imports?${qs.toString()}`, {
    method: 'POST',
    credentials: 'same-origin',
    body: form,
  })
  if (!res.ok) {
    const body = await res.json().catch(() => null)
    throw new ApiError(res.status, body?.detail ?? res.statusText)
  }
  return res.json() as Promise<T>
}

export const previewImport = (ws: string, accountId: string, file: File) =>
  upload<ImportPreview>(ws, accountId, file, false)

export const commitImport = (ws: string, accountId: string, file: File) =>
  upload<ImportResult>(ws, accountId, file, true)
```

- [ ] **Шаг 2: Тест панели превью (падает)**

Create `frontend/src/pages/ImportPreviewPanel.test.tsx`:

```typescript
import { MantineProvider } from '@mantine/core'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { expect, test, vi } from 'vitest'
import type { ImportPreview } from '../api/imports'
import { ImportPreviewPanel } from './ImportPreviewPanel'

const preview: ImportPreview = {
  operations: [
    { occurred_at: '2026-07-04', amount: '-1150.0000', currency: 'RUB', description: 'Перевод', is_duplicate: false },
    { occurred_at: '2026-07-02', amount: '5000.0000', currency: 'RUB', description: 'Пополнение', is_duplicate: true },
  ],
  new_count: 1,
  duplicate_count: 1,
  total_income: '5000.0000',
  total_expense: '1150.0000',
}

function renderPanel(onImport: () => void) {
  return render(
    <MantineProvider>
      <ImportPreviewPanel preview={preview} importing={false} imported={null} onImport={onImport} />
    </MantineProvider>,
  )
}

test('показывает счётчики и строки', () => {
  renderPanel(vi.fn())
  expect(screen.getByText(/Новых:/)).toBeDefined()
  expect(screen.getByText('Перевод')).toBeDefined()
  expect(screen.getByText('дубль')).toBeDefined()
})

test('кнопка импорта зовёт onImport', async () => {
  const onImport = vi.fn()
  renderPanel(onImport)
  await userEvent.click(screen.getByRole('button', { name: /Импортировать/ }))
  expect(onImport).toHaveBeenCalled()
})
```

- [ ] **Шаг 3: Презентационная панель превью**

Create `frontend/src/pages/ImportPreviewPanel.tsx`:

```typescript
import { Alert, Badge, Button, Card, Group, Table, Text } from '@mantine/core'
import type { ImportPreview } from '../api/imports'
import { formatMoney } from '../lib/money'

export function ImportPreviewPanel({
  preview,
  importing,
  imported,
  onImport,
}: {
  preview: ImportPreview
  importing: boolean
  imported: number | null
  onImport: () => void
}) {
  return (
    <Card withBorder>
      <Group justify="space-between" mb="sm">
        <Text>
          Новых: <b>{preview.new_count}</b>, дублей: {preview.duplicate_count}
        </Text>
        <Button disabled={preview.new_count === 0} loading={importing} onClick={onImport}>
          Импортировать {preview.new_count} новых
        </Button>
      </Group>
      {imported !== null && <Alert color="green" mb="sm">Импортировано операций: {imported}</Alert>}
      <Table>
        <Table.Thead>
          <Table.Tr>
            <Table.Th>Дата</Table.Th>
            <Table.Th>Описание</Table.Th>
            <Table.Th ta="right">Сумма</Table.Th>
            <Table.Th />
          </Table.Tr>
        </Table.Thead>
        <Table.Tbody>
          {preview.operations.map((op, i) => (
            <Table.Tr key={i}>
              <Table.Td>{op.occurred_at}</Table.Td>
              <Table.Td>{op.description}</Table.Td>
              <Table.Td ta="right">{formatMoney(op.amount, op.currency)}</Table.Td>
              <Table.Td>
                {op.is_duplicate ? <Badge color="gray">дубль</Badge> : <Badge color="green">новая</Badge>}
              </Table.Td>
            </Table.Tr>
          ))}
        </Table.Tbody>
      </Table>
    </Card>
  )
}
```

- [ ] **Шаг 4: Страница «Импорт»**

Create `frontend/src/pages/ImportPage.tsx`:

```typescript
import { Alert, Button, Card, FileInput, Select, Stack, Title } from '@mantine/core'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { useState } from 'react'
import { commitImport, previewImport, type ImportPreview } from '../api/imports'
import { getAccounts } from '../api/ledger'
import { useWorkspaceStore } from '../store/workspace'
import { ImportPreviewPanel } from './ImportPreviewPanel'

export function ImportPage() {
  const ws = useWorkspaceStore((s) => s.workspaceId)!
  const queryClient = useQueryClient()
  const [accountId, setAccountId] = useState<string | null>(null)
  const [file, setFile] = useState<File | null>(null)
  const [preview, setPreview] = useState<ImportPreview | null>(null)

  const { data: accounts } = useQuery({ queryKey: ['accounts', ws], queryFn: () => getAccounts(ws) })

  const previewMut = useMutation({
    mutationFn: () => previewImport(ws, accountId!, file!),
    onSuccess: setPreview,
  })
  const commitMut = useMutation({
    mutationFn: () => commitImport(ws, accountId!, file!),
    onSuccess: async () => {
      await queryClient.invalidateQueries({ queryKey: ['transactions', ws] })
      await queryClient.invalidateQueries({ queryKey: ['accounts', ws] })
      await queryClient.invalidateQueries({ queryKey: ['dashboard', ws] })
    },
  })

  const reset = () => {
    setPreview(null)
    commitMut.reset()
  }

  return (
    <Stack>
      <Title order={2}>Импорт выписки</Title>
      <Card withBorder>
        <Stack>
          <Select
            label="Счёт"
            placeholder="Куда импортировать"
            data={(accounts ?? []).map((a) => ({ value: a.id, label: a.name }))}
            value={accountId}
            onChange={(v) => { setAccountId(v); reset() }}
          />
          <FileInput
            label="PDF-выписка Т-Банка"
            placeholder="Выберите файл"
            accept="application/pdf"
            value={file}
            onChange={(f) => { setFile(f); reset() }}
          />
          <Button
            disabled={!accountId || !file}
            loading={previewMut.isPending}
            onClick={() => previewMut.mutate()}
          >
            Разобрать
          </Button>
          {previewMut.isError && <Alert color="red">Не удалось разобрать выписку</Alert>}
        </Stack>
      </Card>

      {preview && (
        <ImportPreviewPanel
          preview={preview}
          importing={commitMut.isPending}
          imported={commitMut.data?.imported ?? null}
          onImport={() => commitMut.mutate()}
        />
      )}
    </Stack>
  )
}
```

- [ ] **Шаг 5: Навигация и роут**

В `frontend/src/AppLayout.tsx` в массив `LINKS` добавить (после «Регулярные»):

```typescript
  { to: '/import', label: 'Импорт' },
```

В `frontend/src/main.tsx` импортировать страницу и добавить роут в `children`
под `AppLayout`:

```typescript
import { ImportPage } from './pages/ImportPage'
```
```typescript
          { path: '/import', element: <ImportPage /> },
```

- [ ] **Шаг 6: Прогон**

Run: `cd frontend && pnpm lint && pnpm test && pnpm build`
Expected: зелёное (панель превью: счётчики/строки/кнопка; сборка проходит).

- [ ] **Шаг 7: Коммит**

```bash
git add frontend/src/api/imports.ts frontend/src/pages/ImportPreviewPanel.tsx \
        frontend/src/pages/ImportPreviewPanel.test.tsx frontend/src/pages/ImportPage.tsx \
        frontend/src/AppLayout.tsx frontend/src/main.tsx
git commit -m "Фронт: страница импорта выписки (превью, дедуп-бейджи, подтверждение)"
```

---

## Самопроверка плана (сверка со спекой)

- Модуль `imports` + парсер (§2,§4) — задачи 3, 4, 6. ✓
- Детерминированный парсер формата Т-Банка, чистая `parse_statement` на
  текстовых фикстурах, `extract_lines` (pypdf) (§4) — задачи 3, 4. ✓
- Миграция `0005`: `imports`, `external_id`/`import_id`, уникальный индекс (§5) —
  задача 2. ✓
- Дедуп по хешу `(account_id|дата|сумма|описание)`; повторный импорт → 0 новых
  (§5) — задачи 5, 6. ✓
- Синхронный поток `POST /api/imports?...&commit=` превью/подтверждение (§6) —
  задача 6. ✓
- Импортированные операции `source=import`, `category_id=null` через
  `ledger.service` (§6) — задачи 5, 6. ✓
- Изоляция (чужой счёт 404 / чужой workspace 403); битый PDF 422 — задача 6. ✓
- import-linter контракт для `imports` (§8) — задача 6. ✓
- Frontend: страница импорта, превью, подтверждение (§7) — задача 7. ✓
- Тесты парсера/дедупа/изоляции/фронта (§8) — по задачам. ✓
- Реальный PDF пользователя не в репозитории (PII) — защищён `.gitignore`
  (сделано при написании спеки). ✓

Согласованность: `extract_lines`/`parse_statement`/`ParsedOperation`/
`ParsedStatement`/`StatementParseError`; `external_id`/`import_id`;
`existing_external_ids`/`account_exists`/`post_transaction(..., external_id,
import_id)`; `ImportPreviewOut`/`ImportResultOut`; `previewImport`/`commitImport`
— имена и сигнатуры совпадают между задачами. `workspace_id`/`account_id` —
query везде.

Вне охвата (по спеке §2): другие банки/произвольные форматы (LLM, этап 2);
Celery-импорт; редактирование строк превью; откат импорта; парный transfer.

---

## Передача в исполнение

План сохранён в `docs/superpowers/plans/2026-07-09-etap-1.2b2-imports.md`.

Два режима:
1. **Subagent-Driven (рекомендуется)** — свежий сабагент на задачу, ревью между
   задачами. Саб-скилл: superpowers:subagent-driven-development.
2. **Inline** — пакетами с чекпойнтами. Саб-скилл: superpowers:executing-plans.