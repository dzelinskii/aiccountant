# Личный стенд сессии: тот же compose, но своё имя проекта, свои тома и
# свой порт. Caddy сюда не поднимают — он занимает 80 и 443, и второй стенд
# с ним не встанет; бэкенд отдаётся напрямую.
services:
  backend:
    ports:
      - "${STAND_PORT:-18000}:8000"
