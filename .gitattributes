# Генерируемая часть справочника сверяется в CI побайтово: разъедься переводы
# строк между Windows и Linux, сверка падала бы на пустом месте, а причина была
# бы неочевидна. Этот проект уже наступал на это со сторожем таблицы MCC.
docs/reference/generated/*.md text eol=lf
