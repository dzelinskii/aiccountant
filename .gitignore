# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/

# Окружение
.env

# IDE
.idea/
.vscode/

# Личные банковские выписки (PII) — не коммитить
Справка_*.pdf
*_movement_statement_private.pdf

# Рабочие копии параллельных сессий — внутри полные копии репозитория.
# Сейчас их прячет .git/info/exclude, но он локальный и в свежий клон не попадёт:
# без этого правила случайный git add -A утащил бы репозиторий внутрь себя.
.claude/worktrees/
